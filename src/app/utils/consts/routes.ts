export enum routes {
  PUBLIC_IMAGES = '',
  USER_IMAGES = 'images-control',
  UPLOAD_IMAGE = 'new-image',
  UPDATE_IMAGE = 'update-image',
  REGISTER = 'register',
  LOGIN = 'login',
  ID = '/:id',
}
